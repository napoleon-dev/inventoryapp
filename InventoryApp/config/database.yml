# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#

development:
        adapter: mysql2
        encoding: utf8
        reconnect: false
        database: inventorydb_dev
        pool: false
        username: root
        password: bitnami 
        socket: /tmp/mysql.sock
        host: 127.0.0.1

# The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
     adapter: mysql2
     encoding: utf8
     reconnect: false
     database: inventorydb_test
     pool: false
     username: root
     password: bitnami
     socket: /tmp/mysql.sock
     host: 127.0.0.1

production:
     adapter: mysql2
     encoding: utf8
     reconnect: false
     database: inventorydb_test
     pool: false
     username: root
     password: bitnami
     socket: /tmp/mysql.sock
     host: 127.0.0.1







