<%= form_for(@item_type_category) do |f| %>
  <% if @item_type_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_type_category.errors.count, "error") %> prohibited this item_type_category from being saved:</h2>

      <ul>
      <% @item_type_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :itemtypecategory %><br>
    <%= f.text_field :itemtypecategory %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
